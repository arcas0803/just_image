# just_image CLI example

This is an independent Dart consumer. It requires no native configuration.

```bash
dart pub get
dart run bin/main.dart --output=thumbnail.webp
dart run bin/main.dart input.jpg output.webp
```
