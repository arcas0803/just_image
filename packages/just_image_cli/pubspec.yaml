name: just_image_cli
description: >-
  [DISCONTINUED] Command-line tool for just_image. Use the just_image
  package directly to build CLI tools; this wrapper is no longer maintained.
version: 1.0.4
repository: https://github.com/just-image/just_image
homepage: https://github.com/just-image/just_image
topics:
  - image
  - image-processing
  - cli
  - tools

resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  args: ^2.6.0
  just_image: ^1.0.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  just_image_cli: just_image_cli
