name: just_image_cli
description: >-
  Command-line tool for just_image. Process, resize, convert, and
  inspect images from the terminal.
version: 1.0.0
repository: https://github.com/just-image/just_image
homepage: https://github.com/just-image/just_image
topics:
  - image
  - image-processing
  - cli
  - tools

resolution: workspace

environment:
  sdk: ^3.10.8

dependencies:
  args: ^2.6.0
  just_image: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6

executables:
  just_image_cli: just_image_cli
